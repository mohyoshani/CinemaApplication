@model CinemaHallsVM

<div class="container pb-5">
    <div class="row align-items-center mb-4">
        <div class="col-md-6">
            <h2 class="text-primary"><i class="bi bi-people-fill"></i> cinemaHalls List</h2>
        </div>
        <div class="col-md-6 text-end">
            <a href="~/Admin/cinemaHall/Create" class="btn btn-success shadow-sm">
                <i class="bi bi-plus-circle"></i> Create New cinemaHall
            </a>
        </div>
    </div>

    <div class="card shadow-sm mb-4">
        <div class="card-body">
            <form method="get" class="row g-3" action="~/Admin/cinemaHall/Index/">
                <div class="col-md-8">
                    <input type="text" name="query" class="form-control" placeholder="Search by name ..." value="@Model.Query" />
                </div>
                <div class="col-md-4">
                    <button type="submit" class="btn btn-primary w-100">
                        <i class="bi bi-search"></i> Filter
                    </button>
                </div>
            </form>
        </div>
    </div>

    <div class="card shadow-sm">
        <div class="table-responsive">
            <table class="table table-hover mb-0">
                <thead class="table-dark">
                    <tr>
                        <th class="ps-3">Index</th>
                        <th>Name</th>
                        <th>Location</th>
                        <th class="text-center">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @{
                        int count = 1;
                    }
                    @foreach (var cinemaHall in Model.CinemaHalls)
                    {
                        <tr>
                            <td class="ps-3 fw-bold">@((Model.CurrentPage - 1) * 5 + count)</td>
                            <td>@cinemaHall.Name</td>
                            <td>@cinemaHall.Location</td>
                            <td>
                                <div class="d-flex justify-content-center gap-2">
                                    <a href="~/Admin/cinemaHall/Update/@cinemaHall.Id" class="btn btn-sm btn-outline-warning">
                                        <i class="bi bi-pencil-square"></i> Edit
                                    </a>

                                  
                                    <form action="~/Admin/cinemaHall/Delete/@cinemaHall.Id" method="post" class="delete-form">
                                         
                                        <button type="button" class="btn btn-sm btn-outline-danger btn-delete">
                                            <i class="bi bi-trash"></i>Delete
                                        </button>
                                    </form>

                                    <a asp-area="Admin" asp-controller="Seat" asp-action="Index" asp-route-hallId="@cinemaHall.Id" class="btn btn-info btn-sm">
                                        <i class="bi bi-grid-3x3"></i> Manage Seats
                                    </a>
                                </div>
                            </td>
                        </tr>
                        count++;
                    }
                </tbody>
            </table>
        </div>
    </div>

    <div class="row" style="margin-top: 20px;">
        <div class="col-12 d-flex justify-content-center">
            <nav aria-label="Page navigation example">
                <ul class="pagination justify-content-center">
                    @if (Model.CurrentPage > 1)
                    {
                        <li class="page-item"><a class="page-link" href="/Admin/cinemaHall/Index?page=@(Model.CurrentPage - 1)">Previous</a></li>
                    }
                    @for (var i = 1; i <= Model.TotalPages; i++)
                    {
                        @if (i == Model.CurrentPage)
                        {
                            <li class="page-item active"><a class="page-link" href="/Admin/cinemaHall/Index?page=@i">@i</a></li>
                        }
                        else
                        {
                            <li class="page-item"><a class="page-link" href="/Admin/cinemaHall/Index?page=@i">@i</a></li>
                        }
                    }
                    @if (Model.CurrentPage != Model.TotalPages)
                    {
                        <li class="page-item"><a class="page-link" href="/Admin/cinemaHall/Index?page=@(Model.CurrentPage + 1)">Next</a></li>
                    }
                </ul>
            </nav>
        </div>
    </div>
</div>

<script>
    document.querySelectorAll('.btn-delete').forEach(button => {
        button.addEventListener('click', function () {
            const form = this.closest('.delete-form');
            Swal.fire({
                title: 'Are you sure?',
                text: "You won't be able to revert this!",
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#d33',
                cancelButtonColor: '#3085d6',
                confirmButtonText: 'Yes, delete it!',
                cancelButtonText: 'Cancel'
            }).then((result) => {
                if (result.isConfirmed) {
                    form.submit(); //
                }
            });
        });
    });
</script>

